JWT_SECRET=dev-secret-change-me

# SMTP credentials for real email notifications (Project 2 requirement).
# For Gmail: create an "app password" and use it here, not your normal password.
# If left blank, the server still runs and logs notifications, it just skips sending email.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
